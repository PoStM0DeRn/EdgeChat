services:
  app:
    build: .
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - DATABASE_URL=file:/app/db/custom.db
      - WS_SERVER_URL=http://ws-server:3000
      - NODE_ENV=production
      - PORT=3001
      - HOSTNAME=0.0.0.0
    volumes:
      - db-data:/app/db
      - uploads-data:/app/uploads
    depends_on:
      - ws-server
    restart: unless-stopped

  ws-server:
    build:
      context: .
      dockerfile: server/Dockerfile.ws
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NEXT_INTERNAL_PORT=3001
      - SAAS_URL=http://app:3001
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - ws-server
    restart: unless-stopped

volumes:
  db-data:
  uploads-data:
  caddy-data:
  caddy-config:
